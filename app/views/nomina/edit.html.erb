<h2>Editar nomina</h2>
<br>

<%= form_for @nomina, url: nomina_path(@nomina), html: {method: "patch"} do |form| %>
    <div class="form-group">
        <label>Año y mes</label>
        <input required class="form-control" type="month" id="date" name="date" min="2018-01" value=<%= @date %>>
    </div>
    <div class="form-group">
        <label>Monto total</label>
        <%= form.number_field :total_amount, class: 'form-control', :required => true  %>
    </div>
    
    <%= form.submit class: 'btn btn-primary', value: 'Crear nomina' %>
<% end %>